1. Automatic BGG "thing" enrichment
Fetch extra data from the /thing API endpoint (designers, publishers, mechanics, categories, expansions) to make the dataset much richer for analysis.


2. Normalized schema for analytics
Split the JSON into structured datasets (games, rankings, designers, mechanics, categories) so it's easier to query with SQL, Pandas, or dashboards.
